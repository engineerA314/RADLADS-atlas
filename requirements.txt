# RADLADS-Atlas Requirements
# Install with: pip install -r requirements.txt

# Core ML
# Reproducible stack (stage1 eval 2026-01-22)
# NOTE: torch==2.10.0 requires the nightly/cu128 index URL.
torch==2.10.0
lightning==2.6.0
pytorch-lightning==2.6.0
transformers>=4.40.0
safetensors>=0.4.0

# Attention/Memory Kernels
flash-linear-attention>=0.0.1
triton==3.6.0
assoc-scan==0.0.4  # Provides AssocScan for efficient RNN parallelization (from lucidrains)
# Optional dependency used by assoc-scan's accelerated path:
# assoc_scan/assoc_scan.py imports `accelerated_scan.triton` when use_accelerated=True.
accelerated-scan==0.3.1

# Training Infrastructure
deepspeed==0.18.4
accelerate>=1.0.0
wandb>=0.16.0
ninja>=1.11.0

# Evaluation
lm-eval>=0.4.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
einops>=0.7.0
python-dotenv>=1.0.0
matplotlib>=3.9.0

# Testing
pytest>=7.0.0

# Optional: For 8-bit optimizers
# bitsandbytes>=0.41.0

# Optional: For schedule-free optimizers
# schedulefree>=1.0.0
